import styled from "styled-components";

export const HeaderWrapper = styled.header`
   position: fixed;
   width: 100%;
   height: 10.4rem;
   padding: 1.6rem 8rem;
   background-color: #fff;
   z-index: 1;
`;
